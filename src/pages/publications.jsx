import { HeaderLayout } from 'layouts';
import { PublicationGrid } from 'components';

export const Publications = () => {
  return (
    <HeaderLayout>
      <PublicationGrid />
    </HeaderLayout>
  );
};
